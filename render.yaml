services:
  - type: web
    name: recoil-lobby-server
    runtime: docker
    plan: free
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
    disk:
      name: lobby-data
      mountPath: /app/data
      sizeGB: 1
