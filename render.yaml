services:
  - type: web
    name: recoil-lobby-server
    runtime: docker
    plan: starter
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
    disk:
      name: lobby-data
      mountPath: /app/data
      sizeGB: 1
